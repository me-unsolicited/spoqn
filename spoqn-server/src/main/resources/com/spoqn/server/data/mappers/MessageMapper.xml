<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spoqn.server.data.mappers.MessageMapper">
  <resultMap id="message" type="com.spoqn.server.data.Message">
    <id     property="id"           column="message_id"   typeHandler="com.spoqn.server.data.handlers.UUIDHandler" />
    <result property="text"         column="text" />
    <result property="timestamp"    column="created"      typeHandler="com.spoqn.server.data.handlers.InstantHandler" />
    <result property="user"         column="user_name" />
    <result property="displayName"  column="user_display" />
  </resultMap>

  <sql id="selectAll">
    SELECT
      message.message_id
    , message.text
    , message.created
    , user.user_name
    , user.user_display
    FROM message
    INNER JOIN user ON message.user_name = user.user_name
  </sql>

  <select id="findOne" resultMap="message">
    <include refid="selectAll" />
    WHERE message_id = #{id}
  </select>

  <select id="findAll" resultMap="message">
    <include refid="selectAll" />
  </select>

  <insert id="create" useGeneratedKeys="true" keyProperty="id">
    INSERT message SET
      text = #{text}
    , user_name = #{user}
  </insert>
</mapper>